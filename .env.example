# Set this to /<your-repo-name>/ if hosting at username.github.io/repo-name
# Set this to / if using a custom domain
VITE_BASE_URL=/tournament-site/

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional comma-separated allowlist for admin emails (fallback if profiles table is empty)
VITE_SUPABASE_ADMIN_EMAILS=
